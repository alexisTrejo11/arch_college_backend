<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender for logging to console -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender for teacher-service -->
    <appender name="TEACHER_FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/teacher-service.log</file>
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger for teacher-service -->
    <logger name="microservice.teacher_service" level="info">
        <appender-ref ref="TEACHER_FILE" />
    </logger>

    <!-- Root logger configuration -->
    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
