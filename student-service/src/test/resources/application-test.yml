spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:studenttest;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  boot:
    admin:
      client:
        enabled: false
  autoconfigure:
    exclude: org.springframework.cloud.netflix.eureka.server.EurekaServerAutoConfiguration

eureka:
  client:
    enabled: false

jwt:
  secret:
    key: test-secret-key-must-be-at-least-256-bits-for-hs256-testing-purposes-only
