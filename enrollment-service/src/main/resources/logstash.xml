<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender for logging to console -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender for enrollment-service -->
    <appender name="ENROLLMENT_FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/enrollment-service.log</file>
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger for enrollment-service -->
    <logger name="microservice.enrollment_service" level="info">
        <appender-ref ref="ENROLLMENT_FILE" />
    </logger>

    <!-- Root logger configuration -->
    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
